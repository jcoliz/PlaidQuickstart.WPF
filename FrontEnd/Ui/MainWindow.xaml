<Window x:Class="FrontEnd.Ui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:cef="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
        xmlns:local="clr-namespace:FrontEnd.Ui"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <TabControl>
            <TabItem Header="Accounts">
                <StackPanel>
                    <Button Click="LinkButton_Click">Link</Button>
                    <Button Command="{Binding FetchBalancesCommand}">Fetch</Button>
                    <DataGrid Name="dgAccounts" ItemsSource="{Binding BalancesData}" IsReadOnly="True"></DataGrid>
                </StackPanel>
            </TabItem>
            <TabItem Header="Transactions">
                <StackPanel>
                    <Button Command="{Binding FetchTransactionsCommand}">Fetch</Button>
                    <DataGrid Name="dgTransactions" ItemsSource="{Binding TransactionsData}" IsReadOnly="True"></DataGrid>
                </StackPanel>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
